{% extends 'base.html.twig' %}

{% block title %}Редактирование команды{% endblock %}

{% block body %}
    <div class="container">
        <h2 class="text-center mt-5 mb-3">Редактирование команды</h2>
        <div class="card">
            <div class="card-header">
                <a class="btn btn-outline-info float-right" href="{{ path('app_team_index') }}">
                    Список команд
                </a>
            </div>
            <div class="card-body">
                {% include 'team/_form.html.twig' with {'button_label': 'Обновить', 'button_color': 'btn-outline-success'}  %}
            </div>
        </div>
    </div>
{% endblock %}