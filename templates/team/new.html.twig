{% extends 'base.html.twig' %}

{% block title %}Новая команда{% endblock %}

{% block body %}
    <div class="container">
        <h2 class="text-center mt-5 mb-3">Создать команду</h2>
        <div class="card">
            <div class="card-header">
                <a class="btn btn-outline-info float-right" href="{{ path('app_team_index') }}">
                    Список команд
                </a>
            </div>
            <div class="card-body">
                {{ include('team/_form.html.twig') }}
            </div>
        </div>
    </div>
{% endblock %}