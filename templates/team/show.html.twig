{% extends 'base.html.twig' %}

{% block title %}Команда{% endblock %}

{% block body %}
    <div class="container">
        <h2 class="text-center mt-5 mb-3">О команде</h2>
        <div class="card">
            <div class="card-header">
                <a class="btn btn-outline-info float-right" href="{{ path('app_team_index') }}">
                    Редактор команд
                </a>
            </div>
            <div class="card-body">
                <b class="text-muted">Name:</b>
                <p>{{ team.name }}</p>
            </div>
        </div>
    </div>
{% endblock %}