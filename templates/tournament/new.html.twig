{% extends 'base.html.twig' %}

{% block title %}Новый турнир{% endblock %}

{% block body %}
    <div class="container">
        <h2 class="text-center mt-5 mb-3">Создание турнира</h2>
        <div class="card">
            <div class="card-header">
                <a class="btn btn-outline-info float-right" href="{{ path('app_tournament_index') }}">
                    Список турниров
                </a>
            </div>
            <div class="card-body">
                {{ include('tournament/_form.html.twig') }}
            </div>
        </div>
    </div>
{% endblock %}